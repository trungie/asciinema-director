# .envrc

# Create or activate a virtual environment in the .venv directory
layout python3

# Export variables or add more commands if needed
export PIP_REQUIRE_VIRTUALENV=true  # Ensure pip only installs in this venv

# Load the environment variables

